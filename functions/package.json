{
  "name": "tuijo-functions",
  "version": "1.0.0",
  "description": "Cloud Functions per le notifiche push di Tuijo",
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  }
}
